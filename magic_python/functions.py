def length(obj: object):
	"""Replace the 'len' function. What does 'len' actually mean, anyway?
	* lenny? Is there a 'squiggy' function, too?
	* lenovo?
	* less energetic nuetron?"""
	return len(obj)

def string(obj: object):
	"""Replace the 'str' function. What does 'str' actually mean?
	* strong?
	* store?
	* strip?
	* so to recap?"""
	return str(obj)